# Configuration for XGBoost with ATS-Focused Objective Function

data:
  training_jsonl: data/games_train_with_players_90_norm.jsonl
  validation_jsonl: data/games_val_with_players_norm.jsonl
  prediction_jsonl: data/games_predict_2024_2025_with_players_norm.jsonl

model:
  # Model type identifier
  model_type: xgboost
  
  # XGBoost hyperparameters (using tuned values)
  n_estimators: 100
  max_depth: 2
  learning_rate: 0.01
  subsample: 0.6
  colsample_bytree: 0.7
  reg_alpha: 0.1  # L1 regularization
  reg_lambda: 10.0  # L2 regularization
  
  # Training parameters
  random_state: 42
  n_jobs: -1  # Use all CPU cores
  
  # Early stopping (optional - set to null to disable)
  early_stopping_rounds: null
  eval_metric: rmse
  
  # Custom ATS-focused objective function
  use_ats_objective: true
  ats_penalty_weight: 10.0  # Heavy penalty for getting ATS wrong
  margin_penalty_weight: 0.1  # Light penalty for margin error
  
  # Feature engineering for tree models
  include_diff_features: false  # Trees learn interactions automatically
  
  # Feature exclusions (same as Ridge for fair comparison)
  exclude_features:
    - shared_team_weighted_ts_away
    - away_usage_share_top2
    - shared_implied_home_winprob
    - shared_pace_diff

evaluation:
  # Range for margin analysis
  margin_low: -60
  margin_high: 60

